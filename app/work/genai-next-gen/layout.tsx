import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "GenAI-assisted Next-gen Exploration",
  description: "P90 × GenAI N+1 design language: a two-week CMF sprint from product DNA to an expandable family.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
